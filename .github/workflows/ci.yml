name: "CI Pipeline"

on:
  pull_request:
    types: [opened, synchronize, reopened, closed]

jobs:
  # ✔️ Run tests (includes DB + migrations)
  test:
    uses: ./.github/workflows/test.yml

  # ✔️ Send Discord notifications
  notify:
    needs: test
    uses: ./.github/workflows/notify.yml